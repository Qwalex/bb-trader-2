{
  "name": "@app/userbot",
  "version": "0.0.0",
  "private": true,
  "description": "Python+Telethon userbot. pnpm-scripts просто вызывают uv/python.",
  "scripts": {
    "dev": "uv run python -m bb_userbot.main",
    "build": "echo 'No build needed for Python' && exit 0",
    "typecheck": "uv run mypy src/bb_userbot",
    "lint": "uv run ruff check src/bb_userbot",
    "format": "uv run ruff format src/bb_userbot"
  }
}
